import React from "react";
import { render } from "@testing-library/react";
import RepoTable from "./RepoTable";

describe("<RepoTable />", () => {
  test("should fetch data from the GitHub API and render RepoHeader", () => {});

  test("should set error message when there is an error", () => {});

  test("should move branches from In Progress to In Review", () => {});
});
