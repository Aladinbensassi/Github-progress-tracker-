import React from "react";
import { render } from "@testing-library/react";
import RepoGithub from "./RepoGithub";

describe("<RepoGithub />", () => {
  test("renders the component", () => {});

  test("repoLink state is empty by default", () => {});

  test("repoLink state changes on input change", () => {});

  test("handleSubmit function is defined", () => {});
});
